import {clamp} from "../utils/helpers.js";export function updateKiwiLevel(kiwi){kiwi.level=Math.floor(kiwi.affection/20)+1;}export function bokbokKiwi(state){state.kiwi.affection+=1;state.kiwi.totalBokbok+=1;state.kiwi.thought=pickBokbokThought(state.kiwi.totalBokbok);updateKiwiLevel(state.kiwi);}export function rewardKiwiForStudy(state,amount=2){state.kiwi.affection+=clamp(amount,1,5);state.kiwi.thought="농부가 밭을 갈았습니다. 삐익.";updateKiwiLevel(state.kiwi);}function pickBokbokThought(total){if(total%100===0)return "복복 100단위 달성. 키위는 모릅니다.";if(total%10===0)return "복복을 받았습니다. 삐익.";return "삐익.";}
