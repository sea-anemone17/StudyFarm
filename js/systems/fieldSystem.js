import {uid} from "../utils/uid.js";import {CROPS} from "../data/crops.js";
export function createFieldForSubject(state,subject,cropId="wheat"){const field={id:uid("field"),subjectId:subject.id,name:`${subject.name} 밭`,cropId,growth:0,completedQuestCount:0,totalQuestCount:0,weedCount:0,harvestReady:false,harvestedCount:0};state.fields.push(field);return field;}
export function updateFieldProgress(state,subjectId){const field=state.fields.find(field=>field.subjectId===subjectId);if(!field)return;const tasks=state.tasks.filter(task=>task.subjectId===subjectId);const doneTasks=tasks.filter(task=>task.status==="done");field.totalQuestCount=tasks.length;field.completedQuestCount=doneTasks.length;field.weedCount=state.logs.filter(log=>log.subjectId===subjectId).reduce((sum,log)=>sum+(log.weeds?.length||0),0);field.growth=tasks.length?Math.floor((doneTasks.length/tasks.length)*100):0;field.harvestReady=field.growth>=100&&tasks.length>0;}
export function updateAllFields(state){state.fields.forEach(field=>updateFieldProgress(state,field.subjectId));}
export function harvestField(state,fieldId){const field=state.fields.find(field=>field.id===fieldId);if(!field||!field.harvestReady)return false;const crop=CROPS[field.cropId];if(!crop)return false;const amount=Math.max(1,field.completedQuestCount);state.inventory.crops[field.cropId]=Number(state.inventory.crops[field.cropId]||0)+amount;field.harvestedCount+=1;field.harvestReady=false;return true;}
