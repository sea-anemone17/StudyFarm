export function bindNavigation({getState,setState,render}){document.addEventListener("click",event=>{const mainTab=event.target.closest("[data-tab]");if(mainTab){const state=getState();state.ui.activeTab=mainTab.dataset.tab;setState(state);render();return;}const goTab=event.target.closest("[data-go-tab]");if(goTab){const state=getState();state.ui.activeTab=goTab.dataset.goTab;setState(state);render();return;}const studySubTab=event.target.closest("[data-study-subtab]");if(studySubTab){const state=getState();state.ui.studySubTab=studySubTab.dataset.studySubtab;setState(state);render();}});}
