import {CROPS} from "../data/crops.js";import {WEEDS} from "../data/weeds.js";import {summarizeWeeds} from "../systems/studyLogSystem.js";export function renderStorage(state){const weedCounts=summarizeWeeds(state);return `<main class="screen"><header class="page-head"><div><h1>📦 창고</h1><p>작물, 기록, 백업, 시즌 관리를 모아 둡니다.</p></div></header><section class="card"><h2>작물 창고</h2><div class="inventory">${Object.values(CROPS).map(crop=>`<div class="inventory-item"><span>${crop.icon}</span><strong>${crop.name}</strong><em>${state.inventory.crops[crop.id]||0}개</em></div>`).join("")}</div></section><section class="card"><h2>잡초 기록</h2><div class="inventory">${WEEDS.map(weed=>`<div class="inventory-item"><span>${weed.icon}</span><strong>${weed.name}</strong><em>${weedCounts[weed.id]||0}회</em></div>`).join("")}</div></section><section class="card"><h2>백업</h2><p class="muted">브라우저 localStorage에 자동 저장됩니다. 백업 파일도 내려받을 수 있습니다.</p><button id="exportBackup">백업 내보내기</button></section><section class="card season-warning"><h2>🌾 새 시즌 시작</h2><p>밭, 과목, 퀘스트, 복습, 회고 기록을 시즌 아카이브에 보관하고 새로 시작합니다.</p><p><strong>🥝 키위는 절대 초기화되지 않습니다.</strong></p><button class="danger" id="resetSeason">키위를 남기고 새 시즌 시작</button></section></main>`;}
