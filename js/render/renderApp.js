import {renderKiwi} from "./renderKiwi.js";import {renderStudy} from "./renderStudy.js";import {renderFarm} from "./renderFarm.js";import {renderStorage} from "./renderStorage.js";export function renderApp(state){const app=document.querySelector("#app");const tab=state.ui.activeTab||"kiwi";if(tab==="kiwi")app.innerHTML=renderKiwi(state);if(tab==="study")app.innerHTML=renderStudy(state);if(tab==="farm")app.innerHTML=renderFarm(state);if(tab==="storage")app.innerHTML=renderStorage(state);document.querySelectorAll(".bottom-nav button").forEach(button=>{button.classList.toggle("active",button.dataset.tab===tab);});}
